[build]
  command = "node scripts/generate-sitemap.mjs && npm run build && ./node_modules/.bin/vite build --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender.mjs"
  publish = "dist"
